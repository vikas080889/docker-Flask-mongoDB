version: "3"

services:
  web:
    build: ./web
    volumes:
      - ./web:/usr/src/app
    ports:
      - "5000:5000"
    links:
      - db
  db:
    build: ./db
    volumes:
      - /vol/test/mongodb:/data/db
